# ===========================================
# Backend STAGING – à copier en .env dans C:\app\backend-staging
# Remplir les valeurs ; ne jamais utiliser la DB ou les clés Stripe de la prod.
# ===========================================

# Database (base dédiée staging, ex. saas_mix_staging)
DATABASE_URL=postgresql://user:password@localhost/saas_mix_staging

# Redis (même instance ou dédiée)
REDIS_URL=redis://localhost:6379

# AWS S3 (même bucket avec préfixe staging, ou bucket séparé – au choix)
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_S3_BUCKET=your_bucket_name
AWS_REGION=us-east-1

# Stripe – TEST uniquement (sk_test_..., whsec_... test)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_MONTHLY=price_...
STRIPE_PRICE_ANNUAL=price_...

# Security (clé différente de la prod)
SECRET_KEY=staging_secret_key_change_this

# Mix : même host et VST3 que la prod sur le même VPS (obligatoire pour que le mix fonctionne)
# Si le service NSSM définit déjà ces variables (install-staging-service.ps1), le .env est ignoré pour celles-ci.
HISE_VST3_HOST_EXE=C:\app\hise_vst3_host.exe
VST_BASE=C:\Users\Administrator\Desktop\vps vst3
